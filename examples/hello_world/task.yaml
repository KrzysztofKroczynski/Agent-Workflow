name: hello_world
description: Minimal example — asks the LLM to greet you in JSON.

providers:
  default: ollama
  ollama:
    module: agentflow.providers.ollama
    model: llama3.1
    base_url: http://localhost:11434

settings:
  timeout: 60

output:
  - greeting
