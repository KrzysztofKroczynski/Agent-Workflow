name: deploy_production
priority: 40

input:
  required:
    - artifact_path
    - version
    - output_dir

output:
  - production_url
  - production_manifest_path

conditions:
  skip_if: "not context.get('approved', False)"

settings:
  timeout: 60
